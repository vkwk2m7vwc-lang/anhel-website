# Решения по аудиту — лог

Решения Алексея по находкам редакционного аудита. Источник правды для
того, что применяется, что откладывается, что ждёт его ответа.

## P0 — раунд 1 (решения от 2026-05-14)

| # | Находка | Решение | Статус |
|---|---|---|---|
| 1 | `/` — дубль блока счётчиков в «О компании» | **Убрать.** Также «4 направления» оставить в одном месте | ✅ Сделано — `bc6abcb` |
| 2 | `/products` — тонкий каталог | **Отложить.** Структурная задача, Алексей продумает наполнение отдельно | 📋 Backlog (P1 после запуска) |
| 3 | `/products` + `/service` — пустая правая половина desktop hero | **Переделать в типографический hero** (одна колонка max-w-4xl, без двухколоночной сетки) | ✅ Сделано — `7db772f`, подтверждено Алексеем на preview |
| 4 | `/service` — нет реального фото сервиса | **В backlog.** Решается фотоматериалами от заказчика, не кодом | 📋 Backlog (ждёт фото от заказчика) |
| 5 | `/hero-e` — dev-страница в публичном роутинге | **Удалить из роутинга.** Файлы компонентов в src/ оставить | ✅ Сделано — `26a5933` |

**Карта `/contacts`:** Алексей подтвердил — на проде карта работает,
пустой бокс в скриншотах = headless-артефакт. Не трогать.

### Что сделано в коммитах

- `bc6abcb fix(home)` — убран дублирующий блок счётчиков из `AboutSection`
  (+ неиспользуемый `Stat`-компонент). «4 направления» убрано из счётчиков
  `ProductionSection` (оставлено только в hero), сетка статов 4→3 колонки.
  Проверено: typecheck + build чистые, layout «О компании» и «Производство»
  держится на mobile и desktop.
- `26a5933 fix(routing)` — удалён `src/app/[locale]/hero-e/`, убран
  `/hero-e` из `robots.ts`, обновлён устаревший комментарий в
  `HeroBgCarousel`. Build: 166 статических страниц (было 169), `/hero-e`
  → 404.
- `7db772f fix(hero)` — решение #3: hero на `/products` и `/service`
  переведён в типографический одноколоночный формат (контент в
  `max-w-4xl`, без `md:grid-cols-12`/`col-span-7`). Mobile не затронут.
  Build чистый. Ждёт ОК Алексея на Vercel preview.

### Технический долг (не блокер)

- Осиротевшие i18n-ключи после удаления счётчиков: `home.about.stats.*`
  (`stats_aria`, `objects_caption`, `years_caption`, `directions_caption`,
  `warranty_unit`, `warranty_caption`) и `home.production.stats
  .directions_caption` — в `messages/{ru,en,tr}/home.json`. Безвредны
  (не ломают сборку), почистить отдельным `chore`/`style`-коммитом.

## Пред-авторизация для P1 (от 2026-05-14)

- **Family-landing страницы** (`/products/pumps`, `/control-systems`,
  `/heating-unit`, `/water-treatment`): если обнаружится тот же паттерн
  пустого desktop-hero — применять типографический hero (`max-w-4xl`,
  одна колонка) автономно, без отдельного согласования. Эталон —
  коммит `7db772f`.

## P1 — раунд 1 (аудит от 2026-05-14)

7 P1-страниц проверены. Блокеров нет. Найден один сквозной паттерн —
пустой desktop-hero на двух family-landing — **исправлен автономно по
пред-авторизации** (не требовал отдельного согласования).

| # | Находка | Решение | Статус |
|---|---|---|---|
| P1-1 | `/products/pumps` — пустой desktop-hero (тот же паттерн, что #3) | Типографический hero `max-w-4xl`, эталон `7db772f` | ✅ Сделано — автономно по пред-авторизации |
| P1-2 | `/products/control-systems` — пустой desktop-hero | то же | ✅ Сделано — автономно по пред-авторизации |
| P1-3 | `/documents`, `/projects`, `/service/request`, `/products/heating-unit`, `/products/water-treatment` | Проблемных блоков нет — status ok | ✅ Проверено, правок не требуют |

**Артефакт, не баг:** на `/service/request` full-page скриншот показывает
sticky-навигацию наезжающей на поля формы. Проверено viewport-only
снимками — это headless-артефакт того же рода, что пустая карта на
`/contacts`. Реальному пользователю показывать нечего, правка не нужна.

**Мелочи P1 (косметика, не блокеры, ждут решения если захочется чинить):**
- `/documents` desktop — нечётные группы карточек (5 шт) дают полупустую
  последнюю строку в 2-колоночной сетке.
- `/service/request` mobile — высокая шапка формы + sticky-бар сжимают
  первый экран; дубль подписи шага («ШАГ 1 ИЗ 5» и «ШАГ 1 / 5»).
- `/products/heating-unit` + `/products/water-treatment` — `line-clamp` в
  `AdvantagesGrid` режет смысловую часть описаний у части пунктов.
- `/products/water-treatment` — `BrandsStrip` на mobile неровно выкладывает
  названия брендов (flex-wrap «гуляет»). Правка по компоненту, не по странице.

### Что сделано в коммитах

- `fix(hero)` — `/products/pumps` + `/products/control-systems`: hero
  переведён в типографический одноколоночный формат (`div max-w-4xl`),
  с H1 снят рассогласованный `max-w-[860px]`. Один-в-один приём из
  `7db772f`. Mobile не затронут. `tsc` + `build` чистые.
- `docs(audit)` — per-page `_audit/pages/*.md` + `.blocks.json` на 7
  P1-страниц, `_audit/REPORT-interim-P1.md`, этот лог решений.

## P2 — раунд 1 (аудит от 2026-05-14)

35 P2-маршрутов проверены (типовые представители + перенос). Блокеров нет.
В этой сессии применены 4 пред-авторизованных косметических фикса с P1 и
исправлены 3 реальных бага, найденных в P2 — всё в категории «чинить —
сам», отдельного согласования не требовало.

| # | Находка | Решение | Статус |
|---|---|---|---|
| C1 | `/documents` нечётные группы карточек | последняя карточка тянется на 2 кол. | ✅ `fix:` |
| C2 | `/service/request` высокая шапка + sticky на mobile | сжат mobile-ритм отступов | ✅ `fix:` |
| C3 | `line-clamp` режет описания (heating-unit/water-treatment) | убран в `AdvantagesGrid` + `DocumentsGrid` | ✅ `fix:` |
| C4 | `BrandsStrip` «гуляет» на mobile | 2-кол. сетка <sm, flex-стрип со sm+ | ✅ `fix:` |
| P2-5 | `TechSpecsGrid` наложение текста на mobile (длинные ТТХ) | value `flex-1 justify-end text-right`, `leading-tight` | ✅ `fix(product-page)` |
| P2-6 | `TechSpecsGrid` серая дыра при нечётном числе ТТХ + сетка «Применение» ИТП | filler-ячейки до кратности | ✅ `fix(product-page)` |
| P2-7 | `/projects/[slug]` overflow длинного слова в H1 на mobile | `break-words` на H1 | ✅ `fix(projects)` |

**Решение Алексея (выполнено):**
- `firefighting/scenario-{a,b,c,d}` — Алексей подтвердил удаление (остатки
  Lakhta-сценариев, решили не делать). ✅ Сделано — `fix(routing)`
  `0db8383`: удалены 4 route-файла + scenario-* disallow из `robots.ts`.
  Файлы компонентов `src/components/products/firefighting/scenario-*/`
  оставлены (могут пригодиться). Маршруты → 404, preview подтверждён.

**Артефакт, не баг:** `TechSpecsGrid` иногда рендерится серым боксом на
full-page скриншотах с быстрым скроллом — headless-флейк `whileInView`,
проверено диагностикой (opacity=1 при реальном скролле). Тот же класс, что
карта `/contacts` и sticky-нав `/service/request`.

### Что сделано в коммитах

- `fix:` — 4 пред-авторизованных косметических фикса (C1–C4).
- `fix(product-page)` — `TechSpecsGrid` (наложение + серая дыра) +
  сетка «Применение» ИТП-модулей.
- `fix(projects)` — `break-words` на H1 `/projects/[slug]`.
- `docs(audit)` — `_audit/pages/p2-*.md`, `_audit/REPORT-interim-P2.md`,
  этот лог.

## P3 — раунд 1 (аудит от 2026-05-15)

5 квизов проверены (`/quiz/{pumps,vpu,itp,aupd,control-systems}`). Mobile 390
+ desktop 1440 · RU/EN/TR · light/dark. 4 реальных бага найдено и исправлено
автономно — всё в классе «чинить — сам» / косметика (как P2-5…P2-7),
отдельного согласования не требовало. Блокеров запуска нет.

| # | Находка | Решение | Статус |
|---|---|---|---|
| P3-1 | QuizShell — шапка квиза налезает на фикс-хедер (pumps/vpu/itp/aupd, mobile+desktop) | `pt-24 pb-10 sm:pt-28 sm:pb-16`, ритм как у control-systems/юр.страниц | ✅ `fix(quiz)` `1c8e836` |
| P3-2 | QuizShell — битый i18n-ключ `quiz.{itp,aupd}.description` светит в UI | Чинён fallback `useTranslatedConfig.tr()` — guard на абсолютный путь | ✅ `fix(quiz)` `0a37c99` |
| P3-3 | control-systems — ошибка «Согласие» сплющена `ml-auto` в полоску ~120px (RU 5 строк) | Ошибка вынесена под строку чекбокса, full-width, `pl-7` | ✅ `fix(quiz)` `371d361` |
| P3-4 | control-systems — кнопки навигации < 44px + нет safe-area | `min-h-11` на 3 кнопки, `pb-[calc(1rem+env(safe-area-inset-bottom))]` | ✅ `fix(quiz)` `371d361` |

**Проверено, правок не требует:** валидация во всех 5 квизах × 3 локали
(сообщения одной строкой, overflow нет), тач-таргеты QuizShell (≥44px с
P2-эпохи), sticky-бар первый экран не съедает, localStorage-восстановление,
EN/TR прогружены.

**Требует решения Алексея (не блокер):**
- `control-systems` — дубль подписи шага «ШАГ 1 ИЗ 6» + «ШАГ 1 / 6». Тот же
  дубль, что отметили на `/service/request` в P1 и оставили «ждёт решения» —
  не правлю автономно для единообразия. Чинится одной строкой; решение —
  оставить или снять дубль на обеих формах разом.

## P4 — раунд 1 (аудит от 2026-05-15)

2 юр.страницы (`/privacy-policy`, `/personal-data-consent`). Mobile 390 +
desktop 1440 · RU/EN/TR · light/dark. **Правок не требует — status: ok.**
Шапка отбита от хедера (`pt-24 md:pt-32`), типографика длинного текста на
mobile держится, все 3 локали переведены (реквизиты Оператора намеренно RU
во всех локалях), dark не разваливается, перекрёстные ссылки и связка с
чекбоксом согласия в квизах работают. Блокеров запуска нет.

## Backlog после запуска

- **`/products` — глубина каталога** (#2): показать серии/модули/
  конфигурации прямо на странице каталога, а не только через клик.
- **`/service` — реальные фото** (#4): добавить 1-2 фото сервисных работ
  когда заказчик предоставит материалы.
- **Дубль подписи шага в формах** (P1 `/service/request` + P3
  `control-systems`): снять одну из двух надписей «ШАГ N …» — ждёт решения
  Алексея, чинится одной правкой на обеих формах.
